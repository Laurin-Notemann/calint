{
  "type": "object",
  "properties": {
    "blocks": {
      "type": "object",
      "properties": {
        "block_key_delivery_method": {
          "$ref": "#/definitions/element-select",
          "options": {
            "label": "Pipedrive actions",
            "placeholder": "Select action",
            "message": "Select Pipedrive action",
            "isRequired": true,
            "items": []
          }
        }
      }
    },
    "actions": {
      "type": "object",
      "properties": {
        "cancel_action": {
          "$ref": "#/definitions/action-secondary",
          "options": {
            "label": "Cancel",
            "handler": "cancel"
          }
        },
        "submit_action": {
          "$ref": "#/definitions/action-primary",
          "options": {
            "label": "Save",
            "handler": "request"
          }
        }
      }
    }
  }
}
